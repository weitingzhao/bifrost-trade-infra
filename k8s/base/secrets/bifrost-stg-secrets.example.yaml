# Example — copy to bifrost-stg-secrets.yaml (gitignored), fill from .env, then:
#   kubectl apply -f k8s/base/secrets/bifrost-stg-secrets.yaml -n bifrost-stg
apiVersion: v1
kind: Secret
metadata:
  name: bifrost-stg-secrets
  labels:
    app.kubernetes.io/part-of: bifrost
type: Opaque
stringData:
  # Polygon / Massive (also read as POLYGON_API_KEY by some services)
  MASSIVE_API_KEY: "MYCd1tboShKK7UTYgKN_fjtBrZUr9zhe"
  POLYGON_API_KEY: "MYCd1tboShKK7UTYgKN_fjtBrZUr9zhe"
  # Optional — override YAML ops.auth when set
  OPS_OPERATOR_TOKEN: "platform-operator-dev"
  OPS_ADMIN_TOKEN: "platform-admin-dev"
