# Example — copy to bifrost-dev-secrets.yaml (gitignored), fill real values, then:
#   kubectl apply -f k8s/base/secrets/bifrost-dev-secrets.yaml -n bifrost-dev
# See docs/SECRETS.md. Never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: bifrost-dev-secrets
  labels:
    app.kubernetes.io/part-of: bifrost
type: Opaque
stringData:
  MASSIVE_API_KEY: "REPLACE_ME"
  POLYGON_API_KEY: "REPLACE_ME"
  OPS_OPERATOR_TOKEN: "REPLACE_ME"
  OPS_ADMIN_TOKEN: "REPLACE_ME"
  PLATFORM_SATELLITE_AUDIT_TOKEN: "REPLACE_ME"
  MARKET_DATA_WRITE_TOKEN: "REPLACE_ME"
  REDIS_IB_USERNAME: "trade-prod"
  REDIS_IB_PASSWORD: "REPLACE_ME"
  REDIS_MASSIVE_USERNAME: "trade-prod"
  REDIS_MASSIVE_PASSWORD: "REPLACE_ME"
  PGPASSWORD: "REPLACE_ME"
  GOLDEN_SOURCE_PASSWORD: "REPLACE_ME"
