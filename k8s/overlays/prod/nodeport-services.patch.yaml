# Prod gateway — NodePort 30881 (30880 reserved by bifrost-stg).
apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30881
