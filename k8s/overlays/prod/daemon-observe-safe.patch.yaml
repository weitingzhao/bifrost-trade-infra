# P5B — Prod daemon observe mode: FSM + monitor writes; hedge is simulated only (no IB orders).
# Live auto-trade is out of scope until Owner explicitly re-enables order placement in code.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: daemon
spec:
  replicas: 2
