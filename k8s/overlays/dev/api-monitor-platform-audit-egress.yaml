# Wave 6 — DEV api-monitor → platform STG for satellite audit ingest.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: api-monitor-platform-audit-egress
  labels:
    app.kubernetes.io/component: api-monitor
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: api-monitor
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: bifrost-platform-stg
      ports:
        - protocol: TCP
          port: 8780
