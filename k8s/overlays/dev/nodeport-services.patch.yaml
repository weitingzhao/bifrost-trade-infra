# Vision V1 — bifrost-dev nginx gateway NodePort (STG=30880, PROD=30881, DEV=30882).
apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30882
