# Phase B — single nginx gateway NodePort (SPA + /api/*). Direct api/frontend NodePorts removed.
apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30880
