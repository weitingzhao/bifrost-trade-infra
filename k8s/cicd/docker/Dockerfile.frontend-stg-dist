# TIBM W1 — nginx-only frontend image (prebuilt dist, linux/amd64). Context: stocks/ sibling layout.
FROM nginx:alpine
COPY bifrost-trade-frontend/dist /usr/share/nginx/html
COPY bifrost-trade-frontend/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
