# Phase B — 9 real Trade APIs + frontend; PG/Redis/nginx already in bifrost-stg overlay.
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: bifrost-deliver-stg
  namespace: cicd
  labels:
    bifrost.io/purpose: stg-deliver
spec:
  description: >-
    Build stg images (9 FastAPI domains + Trade Frontend + worker + socket from Gitea), rollout bifrost-stg,
    request Argo sync. Phase B v2 — full stack including daemon, Celery, IB/Massive socket.
  params:
    - name: revision
      type: string
      default: main
    - name: giteaBase
      type: string
      default: http://gitea.cicd.svc.cluster.local:3000/bifrost
    - name: registry
      type: string
      default: registry.cicd.svc.cluster.local:5000
    - name: tag
      type: string
      default: stg
    - name: frontendImage
      type: string
      default: registry.cicd.svc.cluster.local:5000/bifrost-frontend:stg
  workspaces:
    - name: build-context
  tasks:
    - name: clone-core
      taskRef:
        name: bifrost-git-clone-gitea
      params:
        - name: url
          value: $(params.giteaBase)/bifrost-trade-core.git
        - name: revision
          value: $(params.revision)
        - name: subdir
          value: bifrost-trade-core
      workspaces:
        - name: output
          workspace: build-context
    - name: clone-worker
      taskRef:
        name: bifrost-git-clone-gitea
      params:
        - name: url
          value: $(params.giteaBase)/bifrost-trade-worker.git
        - name: revision
          value: $(params.revision)
        - name: subdir
          value: bifrost-trade-worker
      workspaces:
        - name: output
          workspace: build-context
    - name: clone-socket
      taskRef:
        name: bifrost-git-clone-gitea
      params:
        - name: url
          value: $(params.giteaBase)/bifrost-trade-socket.git
        - name: revision
          value: $(params.revision)
        - name: subdir
          value: bifrost-trade-socket
      workspaces:
        - name: output
          workspace: build-context
    - name: clone-api
      taskRef:
        name: bifrost-git-clone-gitea
      params:
        - name: url
          value: $(params.giteaBase)/bifrost-trade-api.git
        - name: revision
          value: $(params.revision)
        - name: subdir
          value: bifrost-trade-api
      workspaces:
        - name: output
          workspace: build-context
    - name: clone-frontend
      taskRef:
        name: bifrost-git-clone-gitea
      params:
        - name: url
          value: $(params.giteaBase)/bifrost-trade-frontend.git
        - name: revision
          value: $(params.revision)
        - name: subdir
          value: bifrost-trade-frontend
      workspaces:
        - name: output
          workspace: build-context
    - name: clone-ui
      taskRef:
        name: bifrost-git-clone-gitea
      params:
        - name: url
          value: $(params.giteaBase)/bifrost-ui.git
        - name: revision
          value: $(params.revision)
        - name: subdir
          value: bifrost-ui
      workspaces:
        - name: output
          workspace: build-context
    - name: stage-api-dockerfile
      runAfter: [clone-core, clone-worker, clone-socket, clone-api]
      workspaces:
        - name: source
          workspace: build-context
      taskSpec:
        workspaces:
          - name: source
        volumes:
          - name: dockerfile-cm
            configMap:
              name: bifrost-api-stg-dockerfile
        steps:
          - name: copy
            image: alpine:3.20
            volumeMounts:
              - name: dockerfile-cm
                mountPath: /cm
            script: |
              #!/usr/bin/env sh
              set -eu
              cp /cm/Dockerfile.api-stg "$(workspaces.source.path)/Dockerfile.api-stg"
    - name: build-all-apis
      runAfter: [stage-api-dockerfile]
      taskRef:
        name: bifrost-kaniko-all-apis-stg
      params:
        - name: registry
          value: $(params.registry)
        - name: tag
          value: $(params.tag)
      workspaces:
        - name: source
          workspace: build-context
    - name: stage-frontend-dockerfile
      runAfter: [clone-frontend, clone-ui]
      workspaces:
        - name: source
          workspace: build-context
      taskSpec:
        workspaces:
          - name: source
        volumes:
          - name: dockerfile-cm
            configMap:
              name: bifrost-frontend-stg-dockerfile
        steps:
          - name: copy
            image: alpine:3.20
            volumeMounts:
              - name: dockerfile-cm
                mountPath: /cm
            script: |
              #!/usr/bin/env sh
              set -eu
              cp /cm/Dockerfile.frontend-stg "$(workspaces.source.path)/Dockerfile.frontend-stg"
    - name: build-frontend
      runAfter: [stage-frontend-dockerfile]
      taskRef:
        name: bifrost-kaniko-frontend-real
      params:
        - name: image
          value: $(params.frontendImage)
      workspaces:
        - name: source
          workspace: build-context
    - name: build-worker-socket
      runAfter: [clone-core, clone-worker, clone-socket]
      taskRef:
        name: bifrost-kaniko-worker-socket-stg
      params:
        - name: registry
          value: $(params.registry)
        - name: tag
          value: $(params.tag)
      workspaces:
        - name: source
          workspace: build-context
    - name: rollout
      runAfter: [build-all-apis, build-frontend, build-worker-socket]
      taskRef:
        name: bifrost-rollout-stg
    - name: gitops-sync
      runAfter: [rollout]
      taskRef:
        name: bifrost-argocd-sync-stg
