# Legacy trade-socket IB manifests (archived)

**Status**: Retired — IBGP3 / IB Gateway Plugin program complete.

Platform `data/ib-gateway` + `redis-ib` replaced these StatefulSets. Manifests kept for reference and rollback documentation only; **not** included in `k8s/base` kustomization.

Previous resources: `ib-market-gateway`, `ib-account-agent`, `ib-operator` (+ headless Services).
