# Example only — real tokens via `make sync-flex-tokens` (NS plugin-flex-query).
# Wave 4 DB hygiene: Flex tokens live in this Secret, not Trade DB plaintext.
apiVersion: v1
kind: Secret
metadata:
  name: bifrost-flex-tokens
  namespace: plugin-flex-query
  labels:
    app.kubernetes.io/part-of: bifrost-flex-query
    bifrost.io/wave: "4"
type: Opaque
stringData:
  FLEX_HOST_TOKEN: ""
  FLEX_SECONDARY_TOKEN: ""
