# Optional — copy kubeconfig to secret before first platform-api rollout:
#   kubectl create secret generic bifrost-platform-kubeconfig \
#     --from-file=bifrost-k3s.yaml="$HOME/.kube/bifrost-k3s.yaml" \
#     -n bifrost-platform-stg
apiVersion: v1
kind: Secret
metadata:
  name: bifrost-platform-kubeconfig
  labels:
    bifrost.io/purpose: platform-stg
type: Opaque
stringData:
  bifrost-k3s.yaml: |
    # Replace with cluster admin kubeconfig (make k3s-fetch-kubeconfig).
